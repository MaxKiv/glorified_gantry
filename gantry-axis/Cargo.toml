[package]
name = "gantry-axis"
version = "0.1.0"
edition = "2024"

[dependencies]
thiserror = "2.0.16"
anyhow = "1.0.100"
tokio = { version = "1.48.0", features = ["full"] }
tracing = "0.1.41"
uom = "0.37.0"
gantry-cia402 = { path = "../gantry-cia402/" }
oze-canopen = { path = "../vendor/oze-canopen/" } 
futures = "0.3.31"

[dev-dependencies]
gantry-demo = { path = "../gantry-demo/" } # For tracing setup

[target.'cfg(unix)'.dev-dependencies]
libc = "0.2"
