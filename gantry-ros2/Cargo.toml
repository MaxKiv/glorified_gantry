[package]
name = "gantry-ros2"
version = "0.1.0"
edition = "2024"

[dependencies]
gantry-cia402 = { path = "../gantry-cia402/" }
gantry-axis = { path = "../gantry-axis/" }
gantry-demo = { path = "../gantry-demo/" } # For tracing setup
futures = "0.3.31"
tokio = { version = "1.48.0", features = ["full"] }
r2r = "0.9.5"
anyhow = "1.0.100"
tracing = "0.1.41"
uom = "0.37.0"
oze-canopen = { path = "../vendor/oze-canopen/" } 

